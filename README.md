# Logify Makers

Frontend for the Logify Makers design services site. The app includes a public catalog view, promo code support, announcements, and an admin panel backed by Supabase.

## Run locally

Prerequisite: Node.js

1. Install dependencies with `npm install`
2. Update the Supabase values in `constants.ts`
3. Start the dev server with `npm run dev`
